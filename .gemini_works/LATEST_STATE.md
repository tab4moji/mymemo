# LATEST_STATE
更新日時: 2026-0710-1257-30 JST

## 現在の状態
見出しアンカー ID のハッシュ化対応に加えて、過去の長いアンカーリンク（例: `#wslがadministratorなのかどうか`）を含むURLで直接アクセスされた場合にも、ハッシュ値を自動生成して新しいハッシュIDの要素にスクロールし、同時にアドレスバーのURLハッシュを短いハッシュ（例: `#siiRmDd0`）に置換（replaceState）する機能を実装した。
ブラウザのキャッシュ対策としてキャッシュバスターを `v=3` に更新し、すべての修正を Git の `origin/main` ブランチへ push して GitHub Pages 上へのデプロイを完了した。

## 完了済みタスク
- `markdown.html` の複数ファイルへの分割とテーマ切り替え機能の実装
- `markdown_parse.js` での marked.js バージョン互換性対応（オブジェクト引数と個別引数の双方に対応）
- `markdown_parse.js` での DOMPurify に対するデータ属性保持（`ALLOW_DATA_ATTR: true`）の適用
- `markdown.html` および `markdown.js` へのアセットキャッシュバスター（`?v=3`）の付与
- `spec_markdown_compat_2026-0625-0843.md` およびログ（`2026-0625-0843-55.md`）の作成
- `markdown.js` にて `[text](url)` リンクの適正な解決（アンカー、外部リンク、ローカル相対マークダウン）
- History API を用いたSPA風の同一ページクエリ遷移、`popstate` 対応、および非同期ロード後のハッシュ自動スクロールの実装
- 見出しアンカー ID を 8文字以内の URL-safe Base64 ハッシュに置き換える FNV-1a 64bit ベースのハッシュ生成関数の実装
- ページ内リンクおよび他ドキュメントへの相対リンクのアンカーハッシュ置換処理の実装
- 旧来の長いハッシュ付きURLからのアクセス時の自動ハッシュ変換・スクロール＆アドレスバーURLのハッシュ置換（`replaceState`）機能の実装
- 変更内容をリモートリポジトリへ `push`（GitHub Pagesへのデプロイ）

## 次のタスク
- ユーザーにGitHub Pages上の最新ビルドで、旧ハッシュ付きURLからのアクセス時に自動的にハッシュが短くなりスクロールする動作を確認してもらう。
