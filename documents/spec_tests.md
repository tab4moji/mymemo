# テスト仕様書 (spec_tests.md)

⚠️ このドキュメントはリバースエンジニアリングにより自動生成された。

## テスト仕様・検証項目

以下は、リファクタリング後の各機能が正しく動作することを確認するための検証項目である。

---

## TC-001: URL パラメータによるファイル決定
### 前提条件
- リファクタリング後の `markdown.html` がサーバーまたはローカルで開ける状態。
- `aipc.md` などのテスト用 Markdown ファイルが同ディレクトリに存在すること。
### 操作
1. ブラウザで `markdown.html?content=aipc` にアクセスする。
### 期待結果
- ネットワークリクエストで `aipc.md?t=...` が Fetch される。
- 画面に `aipc.md` の内容が表示される。
- ブラウザのタブ名 (`<title>`) が `aipc` に更新される。

---

## TC-002: ネストされたコードブロックの修復テスト (fixNestedCodeBlocks)
### 前提条件
- `markdown_parse.js` がインポートされていること。
### 操作
1. `fixNestedCodeBlocks` 関数に、多重ネストされたバッククォートを持つ Markdown テキストを入力する。
   例:
   ```
   ````
   ```python
   print("hello")
   ```
   ````
   ```
### 期待結果
- 外側のバッククォートが内側より多く調整され、パースが正しく維持される。

---

## TC-003: コードブロックのコピー機能
### 前提条件
- Markdown が正常に表示され、コードブロックが含まれていること。
### 操作
1. 表示されたコードブロックの右上にある「Copy」ボタンをクリックする。
### 期待結果
- クリップボードにコードブロックの内容がコピーされる。
- ボタンのテキストが「Copied!」に一時的 (2秒間) 変化する。

---

## TC-004: コードブロックの折りたたみ (Snip) 機能
### 前提条件
- 20行を超えるコードを含む Markdown ファイルを読み込む。
### 操作
1. ページを表示する。
2. 折りたたまれた箇所の「... snip (N lines hidden) ... Click to expand all」をクリックする。
### 期待結果
- 初期表示では冒頭10行と末尾10行のみが表示され、中間は折りたたまれている。
- クリック後、コードブロック全体が展開され、全行が表示される。

---

## TC-005: テーマ切り替えとスタイル適用
### 前提条件
- `markdown_dark.css` または `markdown_light.css` が正しく構成されていること。
### 操作
1. `markdown.html` をダークテーマで開く（デフォルトはダーク）。
2. （将来的なテーマ切り替え機能またはCSS変更時）ライトテーマに切り替える。
### 期待結果
- 背景色、文字色、コードブロックのヘッダー色などが指定されたテーマ定義通りに変化する。
